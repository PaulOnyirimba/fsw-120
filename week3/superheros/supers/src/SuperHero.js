import React from 'react'
import heroes from './data.json'


class SuperHero extend React.Component {

    render() {
  
    
    return (
        <div>
        <br />
       
        </div>
    )
}
}
export default SuperHero

 {/* <Container fluid>
        <Row>
        <Col></Col>
        <Col xs={1} className="cell"><img style={{height:"80px"}} src={props.imageName} onClick={() => alert(props.catchPhrase)} /></Col>
        <Col className="cell" style={{backgroundColor:props.bgcolor, borderRadius:"25px"}} onClick={() => alert(props.catchPhrase)}>
        <p style={{fontSize:"20px", fontWeight:"bold", color:props.color}}>{props.name}</p>
        </Col>
        <Col></Col>
        </Row>
        </Container> */}